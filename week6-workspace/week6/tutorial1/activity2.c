#include <stdio.h>
#include "functions.h"
#include <ctype.h>

int main() {
    for (int letter = 0; letter <= 127; letter++){
        if (isprint){
            printf("%c\n", letter);
            printf("%3d\n", letter);

        } else{
            printf("duh \n");
        }
        
    }

    return 0;
}